{
  "name": "BranchSettings",
  "type": "object",
  "properties": {
    "branch": {
      "type": "string",
      "description": "Branch identifier (e.g. '01 McAllen')"
    },
    "invoicePrefix": {
      "type": "string",
      "description": "Short prefix for invoice numbers (e.g. 'MCL', 'WES')"
    },
    "nextInvoiceNumber": {
      "type": "integer",
      "default": 1000,
      "description": "The next invoice number to assign for this branch"
    }
  },
  "required": [
    "branch"
  ]
}